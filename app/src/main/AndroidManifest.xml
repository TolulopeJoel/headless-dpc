<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <application
        android:testOnly="true"
        android:label="Tolu's DPC"
        android:allowBackup="false">

        <!-- Device Owner receiver — no launcher intent filter = invisible -->
        <receiver
            android:name=".AdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policies" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Schedule receiver: handles alarms + boot restore -->
        <receiver
            android:name=".ScheduleReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="com.tolu.dpc.ACTION_RESTRICT" />
                <action android:name="com.tolu.dpc.ACTION_UNRESTRICT" />
    </intent-filter>
</receiver>

        <!-- Setup activity — no launcher intent filter = invisible to drawer -->
        <activity
            android:name=".SetupActivity"
            android:exported="true" />

        <activity
            android:name=".RemoveActivity"
            android:exported="true" />

    </application>
</manifest>